server:
  port: 9093

spring:
  profiles:
    active: local
  application:
    name: USER-SERVICE
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    database-platform: org.hibernate.dialect.MySQLDialect  
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver