server:
  port: 9093
spring:
  application:
    name: USER-SERVICE
  profiles:
    active: dev
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.MySQLDialect  